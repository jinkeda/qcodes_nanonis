"""Tunnelling-condition preparation and restoration workflows (planned).

Reusable procedures for reading bias / current setpoint / feedback state,
ramping bias and current setpoint (with explicit ramp rates, a zero-crossing
policy, cancellation and timeout), enabling/disabling Z feedback safely, waiting
for current/Z stabilization, restoring initial tunnel conditions, and optionally
withdrawing or holding the tip before risky operations.

Not yet implemented. See ``reports/blueprint.md`` (Adoption backlog) for scope.
"""
