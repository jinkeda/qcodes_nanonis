"""Atom tracking and hyperscanning workflows (planned).

Higher-level recipes: ``atom_tracking(duration, settings)`` as a safe context
manager, grid/spiral coordinate generation, scan-each-tile, optional
Z-range/contact verification, and incremental map persistence so a long run can
resume after failure.

Not yet implemented. See ``reports/blueprint.md`` (Adoption backlog) for scope.
"""
